module.exports.help = function help() {
    console.log(`List of all the commands
                1. tpp view src -t
                2. tpp view src -f
                3. tpp untreefy src dest
                4. tpp treefy src dest    `);
}